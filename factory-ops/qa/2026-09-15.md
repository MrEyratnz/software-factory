# QA nightly report — 2026-09-15

## Deterministic suite

- `bash tests/run-suite.sh`: **green**
  - hooks contract: 361 passed, 0 failed
  - build (`claude plugin validate --strict` on both manifests): green
- Head unchanged since the 2026-09-14 report (`b1a19d2`, PR #1512 / #442 fix
  + reconcile checkpoint).
- Note: a `fatal: empty ident name ... not allowed` line appears mid-run,
  from a test fixture's own subprocess `git commit` failing to find a global
  git identity in this runner image. It does not affect the suite verdict —
  the surrounding assertions (#442 worktree-isolation coverage) still pass,
  and the run ends `hooks contract: 361 passed, 0 failed`. Not a regression;
  same as prior nightly runs on this image.

## Behavioral-eval harness

`factory-ops/qa/evals/` still does not exist — no eval layer to run, no
trigger/outcome baselines to compare. Tracked upstream in #1139/#161.

## Issues filed

None. No suite failures, no eval regressions (no eval layer), no new bug
signal since the last report.

## Known station gap

Same failure mode as every prior nightly run since 2026-07-24: this
station's bot identity (`dsf-qa-mreyratnz[bot]`) cannot land its own report
directly — `main` is branch-protected (direct push rejected, expected).
Tracked in #1540, still open. Report committed to branch
`chore/qa-nightly-2026-09-15`, pushed; needs a human or higher-privileged
session to open the landing PR.
