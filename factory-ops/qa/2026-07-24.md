# QA nightly report — 2026-07-24

## Deterministic suite (`tests/run-suite.sh`)

**Result: green.** Ran locally against `main` (HEAD `fbc1867`), matches the
reported nightly result.

| Stage | Result |
|---|---|
| typecheck (bash -n over shell entrypoints) | pass |
| boundaries (scaffold/bootstrap contract, config schema, triage-script contracts) | pass |
| unit (connector: `node --test`) | pass |
| bdd (hooks contract) | 352 passed, 0 failed |
| build (`claude plugin validate --strict`, both manifests) | pass |

No new red, no flakes observed (single run — suite is deterministic per its
own header comment, so no repeat-run was needed for confirmation).

## Behavioral-eval harness (layer 3)

`factory-ops/qa/evals/` does not exist yet — no eval harness to run. This is
expected: it's tracked as open work, not a regression. No baseline to compare
against, so no threshold check performed tonight.

- Epic 1.2 (coverage gate ≥95% lines on `hooks/scripts/**`): #160, open, not
  yet enforced in `run-suite.sh`.
- Epic 1.3 (behavioral evals, trigger/outcome thresholds): #161, open, not
  yet built.

## Issues filed tonight

None. No suite failure, no eval harness to regress, no new flake.

## Trend

First nightly report on file for this repo (`factory-ops/qa/` had no prior
`.md` reports, only `.gitkeep`) — establishing this as the baseline entry.
Suite green tonight; next report should diff against this one once #160/#161
land.
