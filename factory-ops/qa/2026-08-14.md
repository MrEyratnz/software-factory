# QA nightly — 2026-08-14

## Deterministic suite (`tests/run-suite.sh`) — PASS

| Layer | Result |
|---|---|
| typecheck (`bash -n` over every shell entrypoint) | pass |
| boundaries (scaffold + bootstrap + observability + config schema + triage-script contracts) | 50/50 pass |
| static/config validation (observability contract) | 67/67 pass |
| unit (connector: zero-dep `node --test`) | 29/29 + 54/54 pass |
| bdd (hermetic hook contract tests) | 361/361 pass |
| build (`claude plugin validate --strict` on both manifests) | pass |

Overall: **suite green**, 0 failures, 0 flakes observed (single local
re-run reproduced the same result reported by tonight's CI run on main).
No new `bug` issues filed. Hooks-contract count is up from 358 (2026-08-13)
to 361 — consistent with issue #442's worktree-isolation regression tests
landing (PR #1512) since the last nightly.

## Coverage gate (spec criterion 7, ≥95% lines on `hooks/scripts/**`)

Still not implemented — Epic 1 acceptance criterion "Coverage gate ≥95%
lines fails the suite when unmet" remains unchecked in
`docs/specs/epic-1/spec.md`. Not a regression: no coverage instrumentation
is wired into `tests/run-suite.sh` yet. No change from prior nightly.

## Behavioral-eval harness (layer 3, nightly)

`factory-ops/qa/evals/` still does not exist — layer 3 (trigger/outcome
evals per skill/command, `nightly-eval.yml` thresholds) is unimplemented
per the Epic 1 spec's unchecked acceptance criteria. No eval run, no
baseline to compare against, nothing to trend. No change from prior
nightly.

## Issues filed tonight

None — deterministic suite green, no eval layer to regress.

## Context (not tonight's regressions, pre-existing)

12 open issues currently carry the `bug` label (oldest: #94, #95; most
recent: #688), unrelated to tonight's run — the deterministic suite covers
hook/contract/build layers, not the areas these track. Release Gate
criterion 1 (zero open `bug` issues) is a release-captain/product-owner
concern, out of scope for this nightly report; noted here only for
trend-visibility. Unchanged from 2026-08-13.

## Trend vs. baseline (2026-08-13 → 2026-08-14)

- boundaries: 50 → 50 (flat)
- observability/static: 67 → 67 (flat)
- unit: 29+54 → 29+54 (flat)
- bdd/hooks contract: 358 → 361 (+3, new coverage from #442 fix, PR #1512)
- build: clean → clean (flat)

No threshold regressions to report; there is no eval baseline to compare
against yet.

## Next nightly

Once `factory-ops/qa/evals/` and the coverage-gate stage land (roadmap:
"Hook unit tests: stdin JSON fixtures per event type, exit-code..." per
`/factory-status`), this report should start comparing eval trigger/outcome
rates against a cached baseline and coverage % against the 95% floor. Until
then, nightly QA is limited to the deterministic suite pass/fail check above.
