# QA nightly report — 2026-08-29

## Deterministic suite (`tests/run-suite.sh`)

**Result: green.** Confirmed by re-running locally against `main` @ `b1a19d2`
(exit 0), matching the reported nightly outcome.

| Stage | Result |
|---|---|
| hooks contract (BDD) | 361 passed, 0 failed |
| build (`claude plugin validate --strict`, both manifests) | pass |

No red stage. No flake observed (single run matched nightly CI result; no
intermittent failures to reproduce). No `bug` issues filed.

## Behavioral-eval layer

`factory-ops/qa/evals/` still does not exist — the eval harness (layer 3) is
not built. Nothing to run, no baseline to compare against, no threshold
regression possible tonight. Same gap as every prior nightly on record
(tracked in #1139/#161).

## Coverage law

`docs/ROADMAP.md` still shows the ≥95%-line coverage gate on
`hooks/scripts/**` (Release Gate criterion 7) as unchecked, and
`tests/run-suite.sh` has no coverage stage. Not a regression. No new issue
filed tonight.

## Trend vs. baseline

No cached eval baseline exists yet, so no trend to report this cycle. Suite
pass count (361 BDD) matches the last several nightlies on record
(2026-08-20 through 2026-08-27, all green at the same count) — stable, no
drift.

## Gap: no report for 2026-08-28

No `factory-ops/qa/2026-08-28.md` and no `qa/nightly-2026-08-28` branch exist
on `origin` — last night's run appears to have not happened or not landed
even a branch (unlike every other night since 2026-08-20, which at minimum
produced an unlanded branch per #1540). Not investigated further tonight
(outside this station's blast radius to diagnose scheduler misses); flagging
here so it's visible in the trend record rather than silently skipped.

## Nightly-report landing gap (#1540)

The QA station's bot identity (`dsf-qa-mreyratnz[bot]`) still cannot push
directly to `main` (branch protection) or open a PR (installation token
lacks `pull-requests: write`). Confirmed again tonight: `gh pr create` fails
with `GraphQL: Resource not accessible by integration (createPullRequest)`.
Every nightly report since 2026-08-20 is sitting on an unmerged
`qa/nightly-*` branch, none of them ever merged into `main`
(`git log -- factory-ops/qa/` on `main` shows no report has landed since the
scaffolding commit). This is not a new finding — #1540 already tracks it —
so no duplicate issue filed; commenting on #1540 instead to record the
2026-08-28 gap and that the backlog of unlanded report branches keeps
growing.

## Issues filed tonight

None new. Suite green, no flakes, no eval harness to regress. Commented on
existing #1540 to record the 2026-08-28 gap and another night of the
landing-gap recurrence.
