# QA nightly report — 2026-08-01

## Deterministic suite (layers 1–2)

Result on main: **success** (per nightly-eval.yml `suite` job).

Reproduced locally against `4414ac3` (`bash tests/run-suite.sh`): green.

| Stage | Result |
|---|---|
| typecheck (`bash -n` over shell entrypoints) | pass |
| boundaries (scaffold/bootstrap/observability contracts, config schema, triage-script contracts) | pass |
| unit (connector: `node --test`) | pass |
| bdd (hooks contract) | pass — 358 passed, 0 failed |
| build (`claude plugin validate --strict`, both manifests) | pass |

No new red, no flake observed. No `bug` issue filed for suite health tonight.

## Behavioral evals (layer 3)

**Not run.** The harness this step depends on (`factory-ops/qa/evals/`) does not
exist yet — Epic 1.3 (#161, behavioral evals: trigger + outcome, nightly
thresholds) is still open, and the numeric thresholds themselves are still TBD
(#511, filed against the Release Gate spec). Until #161 lands a harness and
#511 pins concrete threshold values, there is no trigger/outcome rate or
baseline to compare against, and no threshold to regress. No trend, no
baseline delta, no issue filed for this section.

## Coverage law

No coverage regression signal available tonight for the same reason as
above — the ≥95%-line threshold on `hooks/scripts/**` is enforced as part of
the (not-yet-built) eval/coverage layer per #161/#511, not by
`tests/run-suite.sh`'s current stages. Nothing to report as red.

## Issues filed tonight

None. Suite green, no flakes, no eval harness to run yet.

## Notes for the record

- Landing this report hit the known gap in #125 (QA has `contents:write` but
  no path to protected `main`, and no `pull-requests:write` to open a PR for
  it). Already tracked; not re-filed.
- Blocking dependencies for future nightlies to do more than layers 1–2:
  #161 (build the eval harness) and #511 (pin the threshold values it should
  be checked against).
