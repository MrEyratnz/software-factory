# QA nightly report — 2026-07-31

**Station:** qa (nightly)
**Main at:** 7e3c5ce (2026-07-30T21:39:44Z)
**Deterministic suite:** success (as reported), re-verified locally.

## 1. Deterministic suite (`bash tests/run-suite.sh`)

Re-ran locally against `main` to confirm the reported result:

- hooks contract: **358 passed, 0 failed**
- build suite (`claude plugin validate --strict` on both manifests): **pass**
  - plugin manifest: valid
  - marketplace manifest: valid
- overall: **suite green**

No red stage found; nothing to reproduce, no `bug` issue filed for this run.

Note: the hooks-contract run prints a git-identity error
(`fatal: empty ident name ... not allowed`) as part of one of its own test
fixtures (an "author identity unknown" scenario the harness exercises
in-repo) — this is expected fixture output, not a suite failure. The suite's
own summary line (`358 passed, 0 failed`, `suite green`) is authoritative.

## 2. Behavioral-eval harness

`factory-ops/qa/evals/` does not exist yet (only `factory-ops/qa/.gitkeep` is
present under `factory-ops/qa/`). There is no eval layer to run and no
cached baselines to compare against, so step 2 (threshold vs. baseline
regression check) is **not applicable** tonight.

This gap is already tracked: issue #511, "Value the nightly eval thresholds
the v1.0.0 Release Gate depends on (owner: qa)," opened 2026-07-30, covers
building out this harness. No new issue filed to avoid duplicating #511.

## 3. Issues filed tonight

None. Suite green, no eval harness to regress-check.

## Trend vs. baseline

No baseline eval metrics exist yet (see §2); nothing to trend. Once #511
lands a harness + cached baseline, this section will report trigger/outcome
rate deltas.
