# QA nightly report — 2026-08-05 (UTC)

**Suite (`tests/run-suite.sh`) on `main`: green.** HEAD `d2e1a83`
(bump-github-actions merge, 2026-08-04). No red stage, nothing to reproduce,
no `bug` issues filed tonight.

## Layer 3 — behavioral evals

`factory-ops/qa/evals/` does not exist yet — the eval harness itself hasn't
landed, so there is no trigger/outcome data to run or compare against a
baseline tonight. Tracked as the standing prerequisite in #161 (Epic 1.3:
behavioral evals with nightly thresholds); no new issue filed, avoiding a
duplicate.

## Coverage law (≥95% line, `hooks/scripts/**`)

No coverage-measuring stage exists in `tests/run-suite.sh` today (verified —
the "coverage" hits in the suite are hook-classification fixtures for
`coverage run ...` commands, not a coverage tool invocation). There is
nothing to threshold-check yet. This gap is already tracked: #643 (gate
criterion has no implementing mechanism), #591 (criterion presumes tooling
that doesn't exist), #799/#872/#865 (recent scope disputes over what
`hooks/lib/common.sh` should count toward it). No new issue filed.

## Filed tonight

None. Suite green, no eval harness to regress-check, no coverage mechanism
to threshold-check — both gaps already have open tracking issues (#161,
#643 and related).

## Trend vs. baseline

No cached eval baseline exists yet (harness not built) — nothing to trend.
