# QA nightly report — 2026-08-25 (UTC)

## Deterministic suite (`tests/run-suite.sh`)

**Result: success.**

Stage order per `tests/run-suite.sh` (typecheck → boundaries → unit → bdd →
build) — all stages passed on `main`. No red to reproduce, no `bug` issue
filed.

## Behavioral eval layer (Epic 1 layer 3)

**Not run — harness not yet landed.** `factory-ops/qa/evals/` does not exist
in this tree; `.github/workflows/nightly-eval.yml` documents this layer as
gated on that harness landing. No cached baselines exist to trend against.
No threshold regression possible to file this run.

## Coverage law

No coverage regression signal available this run (the ≥95%-line threshold
per `docs/specs/epic-1/spec.md` § "Release Gate for v1.0.0" criterion 7 is
enforced inside the suite itself; the suite was green, so no failing test
surfaced here).

## Trend vs. baseline

First report in `factory-ops/qa/` — no prior nightly report exists to trend
against. This report is the baseline going forward.

## Issues filed

None.
