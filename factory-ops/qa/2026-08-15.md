# QA nightly report — 2026-08-15

## Deterministic suite

- CI (main, tonight's run): **success**.
- Local reproduction (`bash tests/run-suite.sh`): **green**, exit 0.
  - hooks contract: 361 passed, 0 failed
  - build suite: `claude plugin validate --strict` passed on both
    `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json`
- No failing stage to bisect; no P1 `bug` issue filed.

## Behavioral-eval harness

- **Does not exist yet.** `factory-ops/qa/evals/` is absent. Per
  `docs/specs/epic-1/plan.md` §4, the eval harness (trigger prompts,
  programmatic assertions, with-vs-without-plugin baseline lift) is
  implementer-owned to build; qa owns thresholds + flake triage once it
  lands. No cached baselines exist to compare against, so no
  threshold-regression check was possible tonight. No `nightly-eval.yml`
  workflow exists either (per the plan, that's where the full eval suite
  is meant to run).
- No P1 filed for this gap — it's tracked roadmap/plan scope, not a
  regression. (Sprint 5 checkpoint does not currently sequence eval-harness
  standup as a near-term item; flagging here for planner/product-owner
  visibility.)

## Trend vs. baseline

- No prior nightly report exists (`factory-ops/qa/` was empty except
  `.gitkeep` before tonight) — this is the first report, so there is no
  trend to compare. Establishes the baseline file for future nights.

## Issues filed

- None. Suite green, no eval harness to regress.

## Notes

- Tonight's run only reproduces the deterministic contract/build suite
  described in `tests/run-suite.sh`. Once the eval harness exists, this
  report format should gain a per-skill/command trigger/outcome table
  compared against cached baselines, per plan §4.
