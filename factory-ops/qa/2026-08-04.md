# Nightly QA report — 2026-08-04

## Deterministic suite (Epic 1, layers 1+2)

**Result: green.**

- hooks contract: 358 passed, 0 failed
- build suite (`claude plugin validate --strict` on both manifests): passed
  (plugin manifest, marketplace manifest)
- Confirmed independently with a local re-run of `bash tests/run-suite.sh`
  in this session, matching the workflow's `suite` job (run 30895156669,
  conclusion: success).

## Behavioral eval layer (Epic 1, layer 3)

**Not run — harness does not exist yet.** `factory-ops/qa/evals/` is still
an empty scaffold (`.gitkeep` only). Per `docs/specs/epic-1/spec.md`, layer 3
(trigger evals + outcome evals with baseline lift) is required before the
threshold gate in `nightly-eval.yml` can enforce anything. Concrete numeric
thresholds are also not yet defined — tracked by open issue #511 ("Value the
nightly eval thresholds the v1.0.0 Release Gate depends on"), still open,
unchanged tonight. No cached baselines exist to compare against.

## Trend vs. baseline

Deterministic-suite results, last 4 scheduled nightly runs:

| Date (UTC) | `suite` job | `qa-session` job | Report written |
|---|---|---|---|
| 07-31 | success | success | (not checked, predates this session) |
| 08-01 | success | success | (not checked, predates this session) |
| 08-02 | success | **failed** | no |
| 08-03 | success | **failed** | no |
| 08-04 (tonight) | success | this run | yes (this file) |

The deterministic suite itself has been green on every run checked. The
`qa-session` job failed on 08-02 and 08-03 before producing a report — both
failures were the session hitting its weekly usage limit at turn 0
(`api_error_status:429`, `"You've hit your weekly limit"`) before any QA
logic ran, not a suite or eval regression. This is the same failure mode
already tracked by open bug #342 ("Stations exit RED on a usage limit, which
the repo's own usage-limit law forbids") — recurred twice more tonight's
investigation covered; not filing a duplicate, but noting the recurrence
here since it breaks nightly-report continuity that the Release Gate's
"3 consecutive nightly runs green" criterion depends on.

## Issues filed tonight

None. No suite regression to reproduce, and no eval harness to threshold
against. #511 (eval thresholds undefined) and #342 (usage-limit red-exit)
remain open and cover the two gaps found above — verified both are still
open before deciding not to duplicate.

## Release Gate status (informational, owner: release-captain)

Eval-threshold criterion is not yet decidable (harness + thresholds both
missing, #511). Even once it is, tonight's gap in 08-02/08-03 reports means
the "3 consecutive nightly runs" count effectively restarts from tonight.
