# QA nightly report — 2026-08-11 (UTC)

## Deterministic suite

Result: **green** (as reported at wake, independently reproduced this session).

```
env -u FACTORY_OTEL_ENDPOINT bash tests/run-suite.sh
```

Stage results:

| Stage | Result |
|---|---|
| typecheck (`bash -n` over every shell entrypoint) | pass |
| boundaries (scaffold + bootstrap + observability contracts + config schema + triage-script contracts) | pass |
| unit (connector: `node --test`) | pass |
| bdd (hermetic hook contract tests) | pass — hooks contract: 358 passed, 0 failed |
| build (`claude plugin validate --strict` on both manifests) | pass |

No red stage. No P1 `bug` issue filed tonight — nothing to reproduce.

## Behavioral-eval harness

`factory-ops/qa/evals/` does not exist yet — the behavioral-eval layer (trigger
evals per skill/command, ≥3 runs each, trigger-rate thresholds vs. cached
baselines) is still an unchecked `docs/ROADMAP.md` item, not yet built. No eval
run was performed and no baseline comparison was possible. No threshold
regression to file.

## Trend vs. baseline

This is the first entry under `factory-ops/qa/` (previously only `.gitkeep`
existed) — there is no prior nightly report to diff against, so tonight's
result becomes the baseline going forward: suite green, 358/358 hook-contract
tests, all 5 ordered stages pass.

## Other observations (informational only, not a suite/eval finding)

Live open `tech-debt`-labeled issue count via `gh api search/issues` is 1093,
which diverges sharply from the cached SessionStart dashboard figure of 30.
This is outside QA's remit to investigate or fix (QA doesn't file tech-debt
for dashboard/cache staleness, only for suite/eval regressions), but is
worth the next station that owns that dashboard figure checking why the two
numbers have diverged this far.

## Issues filed tonight

None.
