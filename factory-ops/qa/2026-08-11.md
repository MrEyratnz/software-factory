# QA Nightly Report — 2026-08-11

## Layer 1–2: Deterministic suite (`bash tests/run-suite.sh`)

Status: **SUCCESS** (confirmed green on `main` for tonight's run; no local
reproduction needed per the nightly task brief).

No failures observed, so no flake triage or bug filing required for this
layer.

## Layer 3: Behavioral evals (`factory-ops/qa/evals/`)

Status: **NOT PRESENT**. The harness directory does not exist —
`factory-ops/qa/` currently contains no `evals/` subdirectory (only this
report and `.gitkeep`).

This is already tracked as tech-debt: **#1139** — "behavioral-eval harness
(factory-ops/qa/evals/) not stood up — QA cannot check trigger/outcome
regressions." Related open issues on the same gap: #161 (Epic 1.3 behavioral
evals + nightly thresholds, not yet built), #511 (nightly eval thresholds
the v1.0.0 Release Gate depends on — values not yet set), #1029 (eval-
thresholds criterion unbuildable until #511 lands values).

Searched open issues before considering a new filing (`gh issue list
--search "eval harness OR eval threshold OR trigger-rate"`) — the gap is
already captured, so no duplicate was filed. This is a pre-existing
tracked absence, not a new regression, so no additional issue was opened
tonight.

Trigger-rate / outcome-rate thresholds: **not evaluable** (no harness, no
baseline to diff against). No cached baseline exists yet in
`factory-ops/qa/` to compare against — this will be the first entry once
#1139 is resolved and a harness run produces a baseline to cache.

## Coverage law (`hooks/scripts/**` ≥95% line coverage)

Not separately re-run tonight; folded into the layer 1–2 deterministic
suite result above (SUCCESS). No slip observed.

## Issues filed tonight

None. Suite is green; the eval-harness gap is pre-existing tracked
tech-debt (#1139) with no new information to add.

## Trend vs. baseline

No prior QA nightly report exists in `factory-ops/qa/` to trend against —
this is the first dated report in this directory. Establishing baseline
starts once the eval harness (#1139) lands.
