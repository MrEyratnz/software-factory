# QA nightly — 2026-08-18

## Deterministic suite (`bash tests/run-suite.sh`) — **PASS**

Reran locally against `main` @ `b1a19d2` to confirm the reported result; matches (success).

| layer | result |
|---|---|
| typecheck (`bash -n` over shell entrypoints) | ok |
| boundaries (scaffold + bootstrap + observability + config schema + triage) | 50 passed, 0 failed |
| observability contract | 67 passed, 0 failed |
| unit (connector, `node --test`) | ok |
| bdd (hooks contract) | 361 passed, 0 failed |
| build (`claude plugin validate --strict`, both manifests) | ok |

No failures, no flakes observed (single run; suite was already reported green tonight so no repro/triage needed). No `bug` issues filed.

## Behavioral-eval layer

`factory-ops/qa/evals/` does not exist yet — the nightly eval harness has not been stood up. Nothing to run or compare against a baseline. Release Gate criterion 7 requires "three consecutive green `nightly-eval.yml` runs on `main`"; until the harness and workflow exist, that sub-criterion cannot progress. No cached baseline exists to regress against, so no threshold check was possible tonight.

## Coverage law (criterion 7: ≥95% lines on `hooks/scripts/**`, `hooks/lib/common.sh`, `connector/src/release-gate/**`)

Not yet enforced as a suite gate — `tests/run-suite.sh` has no coverage stage, and the corresponding roadmap box ("Coverage threshold ≥95% lines on `hooks/scripts/**`, enforced as a failing test") is still unchecked. This is a known gap, not a regression: nothing to trend against without a first measurement. Flagging so it isn't mistaken for "covered" — implementer work, not filed as a new issue since it's an existing tracked roadmap item, not a fresh finding.

## Trend vs. baseline

This is the first nightly report written under `factory-ops/qa/` (directory previously held only `.gitkeep`, since the initial factory scaffold commit 58eb401) — no prior report to trend against. Establishing this file as the baseline for future nightlies.

## Issues filed

None — suite green, no eval harness to regress, no flakes reproduced.
