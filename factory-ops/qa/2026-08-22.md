# QA nightly report — 2026-08-22

## Deterministic suite (`bash tests/run-suite.sh`)

- **Result: PASS**
- Reran locally against `main` @ `b1a19d2` to confirm the pre-reported
  "success"; matches.
- `hooks contract: 361 passed, 0 failed`
- `build` suite (`claude plugin validate --strict` on both manifests): PASS
- No failures, no flakes observed (single run; suite was already reported
  green tonight so no repro/triage needed).

## Behavioral-eval harness (factory-ops/qa/evals/)

- **Still not stood up.** `factory-ops/qa/evals/` contains only `.gitkeep`;
  no eval layer to run and no cached baselines to diff trigger/outcome rates
  against tonight.
- Already tracked: issue #1139 (tech-debt: harness not stood up) and issue
  #161 (Epic 1.3: behavioral evals with nightly thresholds). No duplicate
  filed.
- Trend vs. baseline: n/a until the harness exists.

## Trend vs. baseline

Prior nightly reports exist but **none have landed on `main`** — they sit
unmerged on branches (`qa/nightly-2026-07-24` through `qa/nightly-2026-08-21`)
because the QA station's token lacks `pull-requests: write` and cannot open
the PR branch protection requires (issue #1540, filed 2026-08-21, still
open). Each prior nightly session, working from unpatched `main`, mistakenly
declared itself "the first report" — that claim in the 2026-08-18 and
2026-08-21 reports is inaccurate; this report corrects it. Until #1540 is
resolved and at least one nightly report actually merges, there is no
baseline reachable from `main` to trend against, only a growing stack of
stranded branches.

## Issues filed tonight

None — suite green, no eval harness to regress. Issue #1540 (PR-rights gap
blocking report landing) is pre-existing and still accurately describes
tonight's situation; not re-filed.

## Notes

- Confirmed tonight: this report hit the same branch-protection/PR-rights
  wall as its predecessors. Pushed to `qa/nightly-2026-08-22` (commit
  `2a2a5fa`); `gh pr create` failed identically to #1540's report
  (`GraphQL: Resource not accessible by integration (createPullRequest)`).
  Needs a human or higher-privileged session to open the PR and land it.
