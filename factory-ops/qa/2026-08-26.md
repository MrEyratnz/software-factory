# QA nightly report — 2026-08-26

## Deterministic suite (`tests/run-suite.sh`)

**Result: green.** Confirmed by re-running locally against `main` @ `b1a19d2`
(exit 0), matching the reported nightly outcome.

| Stage | Result |
|---|---|
| typecheck (`bash -n` over shell entrypoints) | pass |
| boundaries (scaffold/bootstrap/observability contracts, config schema, triage-script contracts) | 50 passed, 0 failed |
| unit (connector: `node --test`) | pass |
| observability contract | 67 passed, 0 failed |
| bdd (hooks contract) | 361 passed, 0 failed |
| build (`claude plugin validate --strict`, both manifests) | pass |

No red stage. No flake observed (single run matched nightly CI result; no
intermittent failures to reproduce). No `bug` issues filed.

## Behavioral-eval layer

`factory-ops/qa/evals/` does not exist yet — the eval harness (layer 3) is
not built. Nothing to run, no baseline to compare against, no threshold
regression possible tonight. This is a gap, not a pass: charter item 2
(compare trigger/outcome rates against cached baselines) is currently a
no-op until the harness lands.

## Coverage law

`docs/ROADMAP.md` still shows the ≥95%-line coverage gate on
`hooks/scripts/**` (Release Gate criterion 7) as unchecked, and
`tests/run-suite.sh` has no coverage stage. Not a regression — it has never
been enforced — but flagging per charter item 4: this is a defined failing
test that doesn't exist yet, not a dashboard number. No new issue filed
tonight (tracked already via the open roadmap item); will file `P0` the
moment a coverage stage exists and slips below threshold.

## Trend vs. baseline

No cached eval baseline exists yet (see above), so no trend to report this
cycle. Suite pass count (361 BDD + 67 observability + 50 boundary) is the
first data point on record in `factory-ops/qa/`; future nightlies should
diff against this file until an eval baseline exists.

## Issues filed tonight

None. Suite green, no flakes, no eval harness to regress.
