# QA nightly report — 2026-09-04

## Deterministic suite (`tests/run-suite.sh`)

- Result: **green** — hooks contract 361/361 passed, 0 failed.
- Build suite (`claude plugin validate --strict` on both manifests): **green**.
- Head at run time: `b1a19d2` (fix(hooks): resolve PROJECT_DIR to the worktree root, not the main checkout (#442) (#1512)).
- No red stage — nothing to reproduce, no P1 filed.

## Behavioral-eval harness

- `factory-ops/qa/evals/` still does not exist — the harness has not been stood up.
- Tracked in #1139 (tech-debt) and #161 (Epic 1.3, P1 enhancement). Both open, no duplicate filed.
- No trigger/outcome thresholds to compare against baseline this run.

## Trend vs. baseline

- No prior QA nightly report exists under `factory-ops/qa/` (this is the first). No trend to compare — future nights can diff against this one.

## Issues filed tonight

- None. Suite green, eval layer not yet available, no new regressions surfaced.

## Notes

- #1540 (QA station token lacks `pull-requests:write`, cannot land its own nightly report past main's branch protection) is still open — this report will hit the same landing problem unless that's been resolved. See commit step below.
