# QA nightly report — 2026-09-14

## Deterministic suite

- `bash tests/run-suite.sh`: **green**
  - hooks contract: 361 passed, 0 failed
  - build (`claude plugin validate --strict` on both manifests): green
- Head unchanged since the 2026-09-12 report (`b1a19d2`, PR #1512 / #442 fix + reconcile checkpoint).

## Behavioral-eval harness

`factory-ops/qa/evals/` still does not exist — no eval layer to run, no
trigger/outcome baselines to compare. Tracked upstream in #1139/#161.
(`.github/workflows/nightly-eval.yml` exists as CI scaffolding but there is
no local eval corpus under `factory-ops/qa/evals/` for this station to run
against.)

## Issues filed

None. No suite failures, no eval regressions (no eval layer), no new bug
signal since the last report.

## Known station gap

Same failure mode as every prior nightly run since 2026-07-24: this
station's bot identity (`dsf-qa-mreyratnz[bot]`) cannot land its own report
directly — `main` is branch-protected (direct push rejected, expected) and
the installation token lacks `pull-requests: write` (`gh pr create` /
direct REST `POST .../pulls` both fail with `403 Resource not accessible by
integration`). Tracked in #1540, still open. Report committed to branch
`chore/qa-nightly-2026-09-14`, pushed; needs a human or higher-privileged
session to open the landing PR.
