# QA nightly report — 2026-07-28

## Deterministic suite (`tests/run-suite.sh`)

**Result: success.** All stages green — typecheck, boundaries (scaffold +
bootstrap contract + config schema + triage-script contracts), unit
(connector), bdd (hook contract). No red stage to reproduce; no bug issues
filed.

## Behavioral-eval harness (`factory-ops/qa/evals/`)

Does not exist yet — `factory-ops/qa/` currently holds only this report
directory (`.gitkeep`, no `evals/` subtree, no cached baselines). Trigger +
outcome eval layer with nightly thresholds is tracked as issue #161, "Epic
1.3: behavioral evals (trigger + outcome) with nightly thresholds" (open,
P1, `area:build`). No eval run performed tonight; no new issue filed since
this gap is already tracked and not a new regression. No baselines exist to
compare against.

## Summary

- Suite: pass, no action.
- Evals: not yet built — see #161. Will run + compare against cached
  baselines once the harness lands.
- Issues filed tonight: none.
