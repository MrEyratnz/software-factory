# QA nightly report — 2026-08-30

## Deterministic suite

- Command: `bash tests/run-suite.sh`
- Result: **green** (matches the pre-run status for tonight's wake)
- Re-run locally to confirm: **green**, `361 passed, 0 failed` (hooks contract
  stage; typecheck/boundaries/unit/bdd/build stages all completed with no
  errors)
- HEAD: `b1a19d2` (2026-08-13T11:57:00Z)
- Trend vs. previous nightly: no prior nightly report exists under
  `factory-ops/qa/` to compare against — this is the first.

Noted but not a failure: the BDD stage prints a git
`"Author identity unknown"` message partway through (from issue #65's
malformed-config test, which spins up a scratch git repo without identity
configured). It's stderr chatter from an intentionally-unconfigured sandbox
repo inside that test case, not a suite failure — the stage still reports
`361 passed, 0 failed` and the overall run ends `suite green`. Not filing;
flagging here in case it's worth quieting in a future pass.

## Behavioral-eval harness

`factory-ops/qa/evals/` does not exist yet — no eval layer to run, no cached
baselines to diff against. Skipped per the nightly protocol's step 2
(harness-exists gate).

## Issues filed

None. Suite green, no eval harness to regress.

## Threshold summary

| Check | Status |
|---|---|
| Deterministic suite | pass |
| Eval trigger/outcome thresholds | n/a (no harness) |
