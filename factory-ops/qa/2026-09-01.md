# QA nightly report — 2026-09-01

## Deterministic suite

- Command: `bash tests/run-suite.sh`
- Result: **green** (matches the pre-run status for tonight's wake)
- Re-run locally to confirm: **green**, `361 passed, 0 failed` (hooks contract
  stage; typecheck/boundaries/unit/bdd/build stages all completed with no
  errors)
- HEAD: `b1a19d2` (2026-08-13T11:57:00Z)
- Trend vs. previous nightly (2026-08-31, commit `787800a`): flat — also
  361/361, build green. No regressions since.

Noted but not a failure: the BDD stage prints a git
`"Author identity unknown"` / `fatal: empty ident name ... not allowed`
message partway through issue #65's malformed-config test case, which spins
up a scratch git repo without identity configured. It's stderr chatter from
an intentionally-unconfigured sandbox repo inside that test, not a suite
failure — the stage still reports `361 passed, 0 failed` and the run ends
`suite green`. Same as prior nights; not filing.

## Behavioral-eval harness

`factory-ops/qa/evals/` does not exist yet — no eval layer to run, no cached
baselines to diff against. Skipped per the nightly protocol's step 2
(harness-exists gate).

## Issues filed

None. Suite green, no eval harness to regress.

## Known gap (not newly filed)

The nightly-report-landing PR-rights gap tracked in #1540 persists (still
open): this station's token lacks `pull-requests:write`, and `main` is
branch-protected, so a direct push to `main` cannot be verified from here.
Not re-filing — #1540 already covers it.

## Threshold summary

| Check | Status |
|---|---|
| Deterministic suite | pass |
| Eval trigger/outcome thresholds | n/a (no harness) |
