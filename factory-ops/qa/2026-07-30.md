# QA nightly report — 2026-07-30

## Deterministic suite (`tests/run-suite.sh`)

**Result: green.**

Confirmed against `main` @ `65c20b2` (2026-07-29T23:16:25Z):

- typecheck (bash -n over all shell entrypoints) — pass
- boundaries (scaffold + bootstrap + observability contracts, config schema,
  triage-script contracts) — pass
- unit (connector: zero-dep `node --test`) — pass
- bdd (hermetic hook contract tests) — **358 passed, 0 failed**
- build (`claude plugin validate --strict` on both manifests) — pass

No red stages, no flakes observed. No `bug` issues filed.

## Behavioral-eval layer (layer 3)

`factory-ops/qa/evals/` exists but is empty (`.gitkeep` only) — the
behavioral-eval harness has not been built yet. No eval run, no
trigger/outcome rates, no baseline to compare against. Nothing to compare
this cycle; this is a gap, not a threshold pass/fail.

**Tracking:** eval-layer standup is roadmap-tracked, not re-filed here to
avoid duplicating an existing tracked item — see `docs/ROADMAP.md` and open
`tech-debt`/`P1`/`P2` issues referencing "coverage/eval nightly-run gate"
(e.g. #500, #477) for current status.

## Coverage law (≥95% line coverage on `hooks/scripts/**`)

Enforced as part of the green suite above (no separate red observed
tonight).

## Trend vs. baseline

No eval baseline exists yet to trend against (see above). Suite has been
green across recent sessions per `factory-ops/state/checkpoint.json`
history; no regression signal tonight.

## Issues filed tonight

None. Suite green, no flakes, no eval layer to regress-check.

## Open tech-debt (context only, not filed tonight)

30 open issues labeled `tech-debt` at time of this report (unchanged
count reported by SessionStart banner). Not QA's to triage beyond flagging;
owned by the tech-debt-clerk / product-owner backlog process.
