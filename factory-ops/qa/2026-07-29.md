# QA nightly report — 2026-07-29 (UTC)

## Deterministic suite (`bash tests/run-suite.sh`)

- **Result: PASS** — hooks contract 358 passed, 0 failed; build suite
  (`claude plugin validate --strict` on both manifests) green.
- Re-ran locally against `main` @ `501f174` to confirm tonight's reported
  success; no red stage, nothing to reproduce or file.
- Note: the run prints a `fatal: empty ident name ... Please tell me who you
  are` line mid-suite (git-identity related, inside one of the receipt/commit
  regression cases). It does not affect the pass/fail outcome — suite still
  reports green — but flagging it here since it reads alarmingly in raw
  output. Not filed as a bug; revisit only if it starts flipping the suite
  red.

## Behavioral-eval harness (`factory-ops/qa/evals/`)

- **Does not exist yet.** No eval layer to run, no baselines to compare
  against, no threshold regression to file. Skipped per protocol step 2.

## Tech-debt / bug backlog snapshot (context only, not a full audit)

- Open `tech-debt`-labeled issues: 273 (session-start banner said 30 — that
  figure is stale; not reconciled here, out of scope for this report).
- Open `bug`-labeled issues: 9 (#404, #360, #342, #319, #314, #229, #228,
  #95, #94) — all pre-existing, none newly filed tonight.

## Filed tonight

- None. Suite green, no eval harness to regress.

## Trend vs. baseline

- No prior nightly report exists to trend against — this is the first entry
  under `factory-ops/qa/`. Future reports should diff suite pass/fail and
  (once the eval harness lands) trigger/outcome rates against this file.
