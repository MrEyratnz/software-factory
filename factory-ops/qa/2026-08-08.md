# QA nightly report — 2026-08-08

## Deterministic suite (`tests/run-suite.sh`)

- Result: **success** (matches the pre-computed nightly result for main).
- Re-ran locally against `main` @ `6b1dfe0` for confirmation:
  `hooks contract: 358 passed, 0 failed`, `build` suite (plugin + marketplace
  manifest `claude plugin validate --strict`) also passed → `suite green`.
- No red stage to reproduce; no new `bug` issue filed.

## Behavioral-eval harness

- `factory-ops/qa/evals/` still does not exist — the eval layer has not been
  stood up, so there is no trigger/outcome baseline to compare against
  tonight. This gap is already tracked as tech-debt (#1139, open, filed
  2026-08-07); not re-filed to avoid a duplicate.

## Trend vs. baseline

- Baseline (established 2026-08-07 @ `6b1dfe0` prior commit): hooks contract
  358 passed / 0 failed, build suite green.
- Tonight (`6b1dfe0`): hooks contract 358 passed / 0 failed, build suite
  green. **No change** — flat, no regression.

## Issues filed

- None. Suite is green, no new regression, eval-harness gap already tracked
  under #1139.
