# QA nightly — 2026-08-06

## Deterministic suite (`tests/run-suite.sh`)

**Result: green.** Ran locally against `main` @ `5940ca9` to confirm the
reported success (`env -u FACTORY_OTEL_ENDPOINT bash tests/run-suite.sh`,
foreground, unmasked):

| stage | result |
|---|---|
| typecheck (`bash -n` over shell entrypoints) | pass |
| boundaries (scaffold/bootstrap/observability contracts, config schema, triage-script contracts) | pass |
| unit (connector: zero-dep `node --test`) | pass |
| bdd (hermetic hook contract tests) | **358 passed, 0 failed** |
| build (`claude plugin validate --strict` on both manifests) | pass |

No red stage. No flakes observed (single run matched the reported nightly
result; nothing here is behaving intermittently). Nothing filed.

## Behavioral-eval harness (layer 3)

`factory-ops/qa/evals/` does not exist yet — the eval layer described in
`docs/specs/epic-1/spec.md` is not built. Confirmed via bootstrap.sh:492-493,
which seeds this as backlog issue **#160** ("Epic 1.2: hook unit tests +
>=95% line coverage gate on hooks/scripts", open, P1) rather than a live
gate. There is no eval trigger/outcome baseline to compare against and no
threshold to enforce tonight. Not a regression — a known, already-tracked
gap; no new issue filed. Related scope-accuracy tech-debt on that same
backlog issue is already tracked at #948 and #1044.

## Coverage law

The charter's ≥95%-line coverage gate on `hooks/scripts/**` is not yet
implemented as a measuring gate in `tests/run-suite.sh` — it's the same
not-yet-built Epic 1.2 item above (#160). No coverage percentage to report
against a baseline tonight; nothing to trend.

## Issues filed tonight

None. Suite is green, no eval harness exists yet to regress, no flakes
reproduced.

## Trend vs. baseline

No baseline cached yet (no eval harness → nothing to baseline). Once #160
lands, this section starts tracking trigger/outcome rate deltas and, if the
coverage gate lands separately, line-coverage % trend.
