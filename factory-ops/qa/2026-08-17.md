# QA nightly report — 2026-08-17

**Station:** dark-software-factory:qa
**Subject commit:** `b1a19d2` (main, HEAD at session start)

## Deterministic suite

Reproduced locally via `bash tests/run-suite.sh`:

- hooks contract: **361 passed, 0 failed**
- build (`claude plugin validate --strict` on both manifests): **pass**
- overall: **suite green**, exit code 0

Matches the reported nightly result (success). No red stage to reproduce,
no P1 `bug` issue filed.

Note: the run prints a benign `Author identity unknown` / `fatal: empty
ident name` line mid-run (issue #442 test scenario exercising git identity
handling inside a throwaway repo fixture) — this is expected stderr chatter
from within a test case, not a suite failure; the suite's own pass/fail
accounting is unaffected.

## Behavioral-eval harness

`factory-ops/qa/evals/` does not exist in this repo yet — no eval layer to
run, no trigger/outcome baselines to compare against. Nothing to file.

## Thresholds

| Check | Result |
|---|---|
| Deterministic suite | PASS (361/361) |
| Build/manifest validation | PASS |
| Behavioral-eval trigger/outcome rates | N/A (harness not yet built) |

## Trend vs. baseline

No prior nightly reports exist under `factory-ops/qa/` (directory was empty
save for `.gitkeep`) — this is the first nightly report on record, so there
is no trend to compare against yet. Future nights can diff against this one.

## Issues filed

None. Suite green, no regressions, no eval harness to threshold-check.
