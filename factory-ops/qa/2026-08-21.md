# QA nightly report — 2026-08-21

## Deterministic suite (`bash tests/run-suite.sh`)

- **Result: PASS**
- `hooks contract: 361 passed, 0 failed`
- `build` suite (`claude plugin validate --strict` on both manifests): PASS
- Re-run locally this session to confirm the pre-reported "success" — matches.

## Behavioral-eval harness (factory-ops/qa/evals/)

- **Not stood up.** `factory-ops/qa/evals/` contains only `.gitkeep`; there is
  no eval layer to run and no cached baselines to diff trigger/outcome rates
  against tonight.
- Already tracked: issue #1139 ("tech-debt: behavioral-eval harness
  (factory-ops/qa/evals/) not stood up — QA cannot check trigger/outcome
  regressions") and issue #161 (Epic 1.3, behavioral evals with nightly
  thresholds). No duplicate filed.
- Trend vs. baseline: n/a until the harness exists.

## Issues filed tonight

None. Suite is green and there is no eval layer to regress-check.

## Notes

- This is the first nightly QA report under `factory-ops/qa/`; no prior
  report exists to trend against.
