# QA nightly report — 2026-08-20 (UTC)

## Deterministic suite (`bash tests/run-suite.sh`)

**Result: success.**

Ran locally against `main` @ `b1a19d2` (fix(hooks): resolve PROJECT_DIR to the
worktree root, not the main checkout (#442), 2026-08-13T11:57:00Z — no
commits landed on `main` since).

All stages green, in order:

- typecheck (`bash -n` over every shell entrypoint)
- boundaries (scaffold + bootstrap + observability contracts, config schema,
  triage-script contracts)
- unit (connector: `node --test`)
- bdd (hooks contract): **361 passed, 0 failed**
- build (`claude plugin validate --strict` on both manifests)

No red stage, no bug filed.

Note: the run emits a benign `Author identity unknown` / `fatal: empty ident
name` line from inside one of the hooks-contract test cases, which simulates
a git author-identity failure as part of its scenario coverage — not an
actual suite failure. The suite's own summary line (`hooks contract: 361
passed, 0 failed`, then `suite green`) confirms this.

## Behavioral-eval harness

`factory-ops/qa/evals/` does not exist yet (only `factory-ops/qa/.gitkeep`
was present before this report). No eval layer to run and no baselines to
compare against — this step is skipped, not failed. Standing up the eval
harness remains open tech-debt for a future sprint.

## Trend vs. baseline

No prior `factory-ops/qa/*.md` nightly report exists — this is the first.
Nothing to trend against yet; future reports should diff suite stage
timings/pass-counts against this one as the initial baseline.

## Issues filed tonight

None. Existing open `bug`-labeled issues (e.g. #688, #423, #422, #404, #360,
#342, #319, #314, #229, #228, #95, #94) are pre-existing and unrelated to
tonight's green run; none were touched.
