# QA nightly report — 2026-08-24

## Deterministic suite (`tests/run-suite.sh`)

**Result: green.** Reproduced locally in addition to the reported nightly
run — result matches (`success`).

| Stage | Result |
|---|---|
| typecheck (`bash -n` over shell entrypoints) | pass |
| boundaries (scaffold/bootstrap/observability contracts, config schema, triage-script contracts) | pass |
| unit (connector: `node --test`) | pass |
| bdd (hermetic hook contract tests) | pass — 361/361 |
| build (`claude plugin validate --strict`, both manifests) | pass |

No red stage, no flake observed. No bug issues filed.

## Behavioral eval layer (Epic 1 layer 3)

`factory-ops/qa/evals/` does not exist yet — the harness has not landed.
Per the nightly-eval workflow's own instructions, this step is a no-op until
that harness ships; there is no cached baseline to compare against and
nothing to enforce tonight. Not filed as a regression (there's no threshold
to miss yet) — the harness itself is a `docs/specs/epic-1/spec.md` §"Release
Gate for v1.0.0" checklist item (trigger + outcome evals per skill/command),
tracked on the roadmap, not a QA-station gap.

## Coverage law watch

The ≥95%-line coverage gate over `hooks/scripts/**` (Release Gate criterion
7) is not yet wired into `tests/run-suite.sh` — it's an unchecked item in
`docs/specs/epic-1/spec.md` (line 42), not a currently-enforced threshold.
Noting it here per the QA charter's coverage law so it doesn't fall off
the trend line once it lands; no new issue filed since it's already an open
roadmap/spec item, not an undetected regression.

## Trend vs. baseline

No cached eval baselines exist to trend against (see above). Suite has been
green across recent nightlies per checkpoint history; no drift to report.

## Issues filed tonight

None.
