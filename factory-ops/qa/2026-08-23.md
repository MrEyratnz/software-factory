# QA nightly report — 2026-08-23

## Deterministic suite (`bash tests/run-suite.sh`)

- **Result: PASS**
- Reran locally against `main` @ `b1a19d2` to confirm the pre-reported
  "success"; matches.
- `hooks contract: 361 passed, 0 failed`
- `build` suite (`claude plugin validate --strict` on both manifests): PASS
- No failures, no flakes observed (single run; suite was already reported
  green tonight so no repro/triage needed).

## Behavioral-eval harness (factory-ops/qa/evals/)

- **Still not stood up.** `factory-ops/qa/evals/` does not exist (only
  `.gitkeep` under `factory-ops/qa/`); no eval layer to run and no cached
  baselines to diff trigger/outcome rates against tonight.
- Already tracked: issue #1139 (tech-debt: harness not stood up) and issue
  #161 (Epic 1.3: behavioral evals with nightly thresholds), both still
  open. No duplicate filed.
- Trend vs. baseline: n/a until the harness exists.

## Trend vs. baseline

Still no nightly report has landed on `main` — they continue to stack up
unmerged on branches (`qa/nightly-2026-07-24` through `qa/nightly-2026-08-22`,
now `qa/nightly-2026-08-23`) because the QA station's token lacks
`pull-requests: write` and cannot open the PR branch protection requires
(issue #1540, filed 2026-08-21, still open, most recently reconfirmed
2026-08-22). No baseline reachable from `main` to trend against tonight,
same as every prior nightly.

## Issues filed tonight

None — suite green, no eval harness to regress. Issue #1540 (PR-rights gap
blocking report landing) is pre-existing and still accurately describes
tonight's situation; not re-filed.

## Notes

- Confirmed tonight: this report hit the same branch-protection/PR-rights
  wall as its predecessors. Pushed to `qa/nightly-2026-08-23`; `gh pr create`
  attempt result recorded below. Needs a human or higher-privileged session
  to open the PR and land it.
