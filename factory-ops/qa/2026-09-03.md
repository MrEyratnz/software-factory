# QA nightly report — 2026-09-03

## Deterministic suite

Ran `bash tests/run-suite.sh` on `main` @ `b1a19d2`: **green**.

- typecheck (bash -n over every shell entrypoint): pass
- boundaries (scaffold + bootstrap + observability contracts + config schema
  + triage-script contracts): pass
- unit (connector: node --test): pass
- bdd (hermetic hook contract tests): **361 passed, 0 failed**
- build (`claude plugin validate --strict` on both manifests): pass

Matches the pre-run status reported at session start ("Tonight's
deterministic suite result on main: success"). No new failures, no P1 `bug`
issues to file for the suite.

## Behavioral-eval harness

`factory-ops/qa/evals/` still does not exist (tracked in #1139/#161). No
eval layer to run, no trigger/outcome baselines to compare, no threshold
regressions to file.

## Trend vs. baseline

No regressions from last night. Suite has been consistently green across
recent nightly runs (see prior reports referenced in issue #1540's comment
thread — 08-21 through 09-02, all green).

## Known open issue: report can't land on `main`

Same failure mode as every night since 2026-08-20 (see #1540): this bot
identity (`dsf-qa-mreyratnz[bot]`) can push a branch but `gh pr create`
fails with `GraphQL: Resource not accessible by integration
(createPullRequest)` — the App installation token lacks
`pull-requests:write`, and `main` is branch-protected against direct push.
This report is being pushed on branch `chore/qa-nightly-2026-09-03`; a
status update is being added to #1540 rather than filing a duplicate issue.
