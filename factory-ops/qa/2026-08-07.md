# QA nightly report — 2026-08-07

## Deterministic suite (`tests/run-suite.sh`)

- Result: **success** (matches the pre-computed nightly result for main).
- Re-ran locally against `main` @ `6b1dfe0` for confirmation:
  `hooks contract: 358 passed, 0 failed`, `build` suite (plugin + marketplace
  manifest `claude plugin validate --strict`) also passed → `suite green`.
- No red stage to reproduce; no new `bug` issue filed.

## Behavioral-eval harness

- `factory-ops/qa/evals/` does not exist yet — the eval layer is not stood
  up, so there is no trigger/outcome baseline to compare against tonight.
- No threshold regression to file. This is a coverage gap, not a pass: QA
  cannot yet detect behavioral (as opposed to deterministic-suite)
  regressions. Filing as tracked tech-debt below.

## This is the first nightly report

`factory-ops/qa/` previously held only `.gitkeep` — no prior report or
baseline exists to trend against. Tonight's 358/0 hooks-contract result
becomes the first cached baseline for future nightly runs to diff against.

## Issues filed

- None for suite regressions (suite is green).
- Filed `tech-debt` issue for the missing eval harness (see below) — this is
  a pre-existing gap this run surfaced, not a new regression, but it blocks
  future nightly threshold checks so it needs tracking per the repo's
  code-review → tech-debt convention.

## Trend vs. baseline

- No baseline existed before tonight. Baseline established: hooks contract
  358 passed / 0 failed, build suite green, on `main` @ `6b1dfe0`
  (2026-08-06T15:54:24Z commit, checked 2026-08-07).
