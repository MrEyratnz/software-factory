---
description: Draft or refresh docs/VISION.md from a product brief — what this is, why it wins (a differentiators table), pillars, and non-goals.
argument-hint: "<brief text | path/to/brief.md>"
---

Turn the brief into `docs/VISION.md`. Brief: `$ARGUMENTS`

Set the write-fence (`echo architect > .factory/active-agent`), then dispatch
the **architect** (write-fenced to `docs/`; load `docs-spine`). The
vision must state, concretely: what this is; a "why it wins" table of
competitors and the weakness you exploit; the product pillars; and explicit
non-goals. Ground it in a real market position — no fluff. This is a docs
station: no code, and the only gate is generated-artifact drift.
