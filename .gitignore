node_modules/
*.log
.DS_Store
dist/
coverage/
*.tgz

# Factory runtime state — generated by the hooks (green receipts, ledger,
# review/panel artifacts); never committed. .factory/config.json (a repo's
# committed factory config) is intentionally NOT ignored.
.factory/state/
.factory/review/
.factory/panel/
.factory/ledger.jsonl
